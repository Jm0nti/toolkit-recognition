# Entornos virtuales
.venv/
.toolkit-venv/

# Cache de Python
__pycache__/
*.pyc

# Dataset augmentado (imágenes)
data/augmented/**/*.png
data/augmented/**/*.jpg
data/augmented/**/*.jpeg

# Salidas de entrenamiento YOLO
# runs/  (omitido por ahora, incluir para informe)

# Artefactos de clasificación (dataset y reportes se regeneran)
outputs/ml_datasets/
#outputs/ml_reports/ (omitido por ahora, incluir para informe)
outputs/predicciones/
#outputs/metricas/         (omitido por ahora, incluir para informe)

# Modelos entrenados (se regeneran)
models/detection/*.pt
models/classification/*.joblib
models/classification/*.pt

# Pesos base preentrenados descargados por Ultralytics
yolo11n.pt
yolov8n.pt

# Data.yaml resuelto (auto-generado)
.data_resuelto.yaml


# Caches de YOLO (se regeneran al entrenar)
*.cache

# Config del editor y fotos de prueba locales (no van al repo)
.vscode/
img-test/

# Dataset masivo descargado (se regenera con run_dataset_masivo.py)
data/masivo/
